<% if product.videos.length > 0 %>
	<%= raw video_reference(product.videos.first).embed_html5({
		:class => 'video-player',
		:id => 'product-video',
		:frameborder => '0',
		:url_params => {
			:enablejsapi => "1",
			:theme => "light"
		}
	}) %>
<% end %>