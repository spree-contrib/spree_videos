<% product.videos.map { |v| video_reference(v) }.each do |video| %>
<%= link_to image_tag(video.thumbnails.first.url, :width => 75), 'Javascript:void(0);', :title => video.title, "data-video-id" => video.unique_id %>
<% end if product.videos.length > 1 %>